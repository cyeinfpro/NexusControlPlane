# Panel SSH endpoint
PANEL_SSH_HOST=198.51.100.10
PANEL_SSH_PORT=22
PANEL_SSH_USER=nexus-tunnel

# Bind tunnel endpoint on panel machine (loopback only)
REMOTE_BIND=127.0.0.1
REMOTE_PORT=28700

# Local agent API endpoint on intranet node
LOCAL_HOST=127.0.0.1
LOCAL_PORT=18700

# SSH key + host verification
SSH_KEY_FILE=/etc/realm-agent/tunnel/id_ed25519
KNOWN_HOSTS_FILE=/etc/realm-agent/tunnel/known_hosts
STRICT_HOST_KEY_CHECKING=yes
